[
  {
    "description": "fixture collected only on the js family (placeholder for platform-specific expectations)",
    "schema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "type": "integer"
    },
    "tests": [
      {
        "description": "an integer is valid",
        "data": 1,
        "valid": true
      },
      {
        "description": "a string is not an integer",
        "data": "1",
        "valid": false
      }
    ]
  }
]
